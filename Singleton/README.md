# Singleton Rules:

1. Atributes + static Class Instance = null.
2. Private Constructor! 
3. 1 function to prove the singleton concept.
4. getInstance(params constructori) - verifica instanta==null?
5. getInstance is static and sync => Singleton Thread Safe (not sure)
